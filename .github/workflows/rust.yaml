on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

name: Lint and Test

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      # Pinned nightly (not floating @nightly) for reproducible lint: this is the
      # rustc 1.98.0-nightly (57d06900f 2026-05-27) the RLC gosh stack is verified
      # against and matches rust-toolchain.toml. Bump deliberately, in lockstep.
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-05-28
          components: clippy, rustfmt
      - run: |
          cargo +nightly-2026-05-28 fmt --all -- --check
          # Check formatting for each example
          for manifest in $(find examples -name Cargo.toml); do
            cargo +nightly-2026-05-28 fmt --manifest-path "$manifest" -- --check
          done

      ## clippy is currently broken, require big refactor
      # - run: cargo clippy -- -D warnings

  test:
    name: Test / Priority Units
    runs-on: ubuntu-latest
    env:
      CARGO_TERM_COLOR: always
      SCCACHE_GHA_ENABLED: "true"
      SCCACHE_GHA_VERSION: ${{ vars.SCCACHE_GHA_VERSION || 1 }}
      RUSTC_WRAPPER: sccache
    steps:
      - uses: actions/checkout@v6
      # Pinned nightly (matches rust-toolchain.toml): this job builds
      # `--features tvm_vm/gosh`, which pulls axiom-eth / snark-verifier-sdk —
      # those require nightly (`trait_alias`). Bump deliberately, in lockstep.
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-05-28
          components: clippy, rustfmt
      - uses: rui314/setup-mold@v1
      - uses: mozilla-actions/sccache-action@v0.0.10
      - name: Setup cargo cache
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-
      - name: Run priority unit tests
        run: |
          cargo test -r --lib --no-fail-fast \
            -p tvm_abi \
            -p tvm_block \
            -p tvm_types \
            -p tvm_block_json \
            -p tvm_sdk \
            -p tvm_vm \
            -p tvm_executor \
            --features tvm_vm/gosh,tvm_executor/signature_with_id

          cargo test -r --tests --no-fail-fast \
            -p tvm_block

  test-client:
    name: Test / tvm_client
    runs-on: ubuntu-latest
    env:
      CARGO_TERM_COLOR: always
      SCCACHE_GHA_ENABLED: "true"
      SCCACHE_GHA_VERSION: ${{ vars.SCCACHE_GHA_VERSION || 1 }}
      RUSTC_WRAPPER: sccache
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: rui314/setup-mold@v1
      - uses: mozilla-actions/sccache-action@v0.0.10
      - name: Setup cargo cache
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-
      - name: Run tvm_client tests
        run: |
          cargo test -r --lib --no-fail-fast \
            -p tvm_client \
            --no-default-features \
            --features tvm_client/std,tvm_client/rustls-tls-webpki-roots

          ## TODO: Enable when mock tests will be ready
          # cargo test -r --test mock_blockchain --no-fail-fast \
          #   -p tvm_client \
          #   --no-default-features \
          #   --features tvm_client/std,tvm_client/rustls-tls-webpki-roots
